// Code generated by api2tf from OpenAPI spec; DO NOT EDIT.

package provider_test

import (
	"fmt"
	"testing"

	"github.com/hashicorp/terraform-plugin-testing/helper/resource"
)

func TestAcc{{ type_name }}Resource_basic(t *testing.T) {
	resource.Test(t, resource.TestCase{
		PreCheck:                 func() { testAccPreCheck(t) },
		ProtoV6ProviderFactories: testAccProtoV6ProviderFactories,
		Steps: []resource.TestStep{
			// Create and read
			{
				Config: testAcc{{ type_name }}ResourceConfig(),
				Check: resource.ComposeAggregateTestCheckFunc(
{% for attr in writable_attributes %}
{% if attr.computability.value == 'required' %}
					resource.TestCheckResourceAttrSet("{{ provider_name }}_{{ noun }}.test", "{{ attr.name }}"),
{% endif %}
{% endfor %}
				),
			},
			// ImportState
			{
				ResourceName:      "{{ provider_name }}_{{ noun }}.test",
				ImportState:       true,
				ImportStateVerify: true,
			},
		},
	})
}

func testAcc{{ type_name }}ResourceConfig() string {
	return fmt.Sprintf(`
resource "{{ provider_name }}_{{ noun }}" "test" {
{% for attr in writable_attributes %}
{% if attr.computability.value == 'required' %}
{% if attr.tf_type.value == 'string' %}
  {{ attr.name }} = "test-{{ attr.name }}"
{% elif attr.tf_type.value == 'int64' %}
  {{ attr.name }} = 1
{% elif attr.tf_type.value == 'float64' %}
  {{ attr.name }} = 1.0
{% elif attr.tf_type.value == 'bool' %}
  {{ attr.name }} = true
{% else %}
  {{ attr.name }} = "test-{{ attr.name }}"
{% endif %}
{% endif %}
{% endfor %}
}
`)
}
