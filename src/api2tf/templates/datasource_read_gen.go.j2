// Code generated by api2tf from OpenAPI spec; DO NOT EDIT.

package provider

import (
	"context"
	"fmt"

	"github.com/hashicorp/terraform-plugin-framework/datasource"
	"github.com/hashicorp/terraform-plugin-framework/types"
	"github.com/hashicorp/terraform-plugin-log/tflog"

	"{{ go_module }}/internal/client"
)

var _ datasource.DataSource = &{{ type_name }}DataSource{}

type {{ type_name }}DataSource struct {
	client *client.Client
}

func New{{ type_name }}DataSource() datasource.DataSource {
	return &{{ type_name }}DataSource{}
}

func (d *{{ type_name }}DataSource) Metadata(_ context.Context, req datasource.MetadataRequest, resp *datasource.MetadataResponse) {
	resp.TypeName = req.ProviderTypeName + "_{{ noun }}"
}

func (d *{{ type_name }}DataSource) Schema(ctx context.Context, _ datasource.SchemaRequest, resp *datasource.SchemaResponse) {
	resp.Schema = {{ type_name }}DataSourceSchema(ctx)
}

func (d *{{ type_name }}DataSource) Configure(_ context.Context, req datasource.ConfigureRequest, resp *datasource.ConfigureResponse) {
	if req.ProviderData == nil {
		return
	}
	c, ok := req.ProviderData.(*client.Client)
	if !ok {
		resp.Diagnostics.AddError(
			"Unexpected Data Source Configure Type",
			fmt.Sprintf("Expected *client.Client, got: %T", req.ProviderData),
		)
		return
	}
	d.client = c
}

func (d *{{ type_name }}DataSource) Read(ctx context.Context, req datasource.ReadRequest, resp *datasource.ReadResponse) {
	var state {{ type_name }}DataSourceModel
	resp.Diagnostics.Append(req.Config.Get(ctx, &state)...)
	if resp.Diagnostics.HasError() {
		return
	}

	// TODO: Implement data source read logic
	// For list endpoints, call the appropriate client method
	// For single-item endpoints, use the ID from state

	tflog.Trace(ctx, "read {{ noun }} data source")
	resp.Diagnostics.Append(resp.State.Set(ctx, &state)...)
}
